{
	"name": "<%=  _.slugify(_.humanize(title))  %>",
	"devDependencies": {<% if (livereload) { %>
		"grunt-browser-sync": "0.9.1",<% } %><% if (csslint) { %>
		"grunt-contrib-csslint": "0.2.0",
		"grunt-contrib-watch": "~0.5.3",<% } %><% if (livereload || csslint) { %>
		"load-grunt-tasks": "~0.2.0"<% } %>
	}
}
